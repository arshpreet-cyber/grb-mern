generator client {
  provider = "prisma-client"
}

datasource db {
  provider = "postgresql"
}

model Review {
  id         Int      @id @default(autoincrement())
  rating     Int
  content    String
  authorName String
  createdAt  DateTime @default(now())
}